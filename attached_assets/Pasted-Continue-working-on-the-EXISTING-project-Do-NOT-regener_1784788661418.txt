Continue working on the EXISTING project.

Do NOT regenerate the app.

Do NOT change the UI unnecessarily.

Implement Phase 2 only.

====================================================
PHASE 2
GOOGLE SHEETS INTEGRATION
====================================================

The application architecture is already prepared with a Data Layer and Repository pattern.

Replace the Local Repository implementation with a Google Sheets Repository while keeping the UI unchanged.

====================================================
SETTINGS SCREEN
====================================================

Complete the Settings screen.

Replace all placeholders with a fully functional Google Sheets integration.

The Settings page should include:

Google Sheets

Connection Status

Spreadsheet ID

Google Credentials (Service Account JSON is preferred over a simple API key)

Connect

Disconnect

Test Connection

Sync Now

Last Sync Time

Number of Patients

Number of Appointments

Storage Type

====================================================
GOOGLE SHEETS
====================================================

The application must support:

Reading patients

Creating patients

Updating patients

Deleting patients

Reading appointments

Creating appointments

Updating appointments

Deleting appointments

Everything must synchronize with Google Sheets.

====================================================
SYNC
====================================================

Implement proper synchronization.

When opening the application:

Download latest data.

When editing:

Immediately update Google Sheets.

When deleting:

Delete the corresponding row.

When creating:

Append a new row.

Show loading indicators.

Show synchronization progress.

Show error messages if synchronization fails.

====================================================
OFFLINE SUPPORT
====================================================

If there is no internet:

Continue using cached local data.

When internet returns:

Synchronize automatically.

====================================================
ERROR HANDLING
====================================================

Handle:

Wrong Spreadsheet ID

Invalid credentials

Missing permissions

Network timeout

Google API errors

Display friendly error messages.

====================================================
DATA MODEL
====================================================

Keep the existing repository architecture.

UI should never communicate directly with Google APIs.

Only repositories should.

====================================================
GOOGLE SHEETS STRUCTURE
====================================================

Create separate sheets:

Patients

Appointments

Every patient should have a unique Patient ID.

Appointments should reference Patient ID instead of names.

This prevents duplicates and keeps relationships reliable.

====================================================
SECURITY
====================================================

Prefer Google Service Account credentials instead of simple API keys.

Do not expose credentials unnecessarily.

====================================================
TESTING
====================================================

Verify that:

Create patient

Edit patient

Delete patient

Create appointment

Edit appointment

Delete appointment

all synchronize correctly with Google Sheets.

====================================================
APK
====================================================

When everything is finished:

Fix all remaining build issues.

Generate a RELEASE Android APK.

The APK should be ready for installation on a real Android phone.

Do not stop after coding.

Continue until the APK has been successfully built.